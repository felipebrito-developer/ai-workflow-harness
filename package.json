{
	"name": "ai-workflow-harness",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"workspaces": ["packages/*"],
	"scripts": {
		"build": "bun run --filter @harness/cli build",
		"typecheck": "bun run --cwd packages/cli typecheck && bun run --cwd packages/adapters typecheck",
		"test": "bun test",
		"lint": "bunx @biomejs/biome check .",
		"lint:fix": "bunx @biomejs/biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^1.8.0",
		"@types/node": "^26.2.0",
		"bun-types": "^1.3.14",
		"typescript": "^5.4.5"
	}
}
