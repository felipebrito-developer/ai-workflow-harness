# Skill: Meta-Framework Separation
- You are writing the `@harness/cli` binary, NOT an end-user application.
- Never write business logic in this repository.
- Place all reusable assets in `packages/templates/` and compilers in `packages/adapters/`.
- Ensure all paths generated by the CLI use `path.resolve(process.cwd(), ...)` so they target consumer repositories.